学员信息
·学号：1901030029
·学习内容：模块的用法
·学习用时：5h

学习笔记：
收获总结：函数的导入from A import B就是把B导入到A文件夹中。知道字典是无法相加的，而列表可以。
遇到的问题：两个字典相加导致无法运行成功。问题已经解决。
解决的过程：字典与字典之间不能相加，但是列表可以。我需要做的是把函数转换成列表，我使用的是强制转换成列表，t1=list(stats_text_en(text))。不过一个朋友说list进行强制字典转换会丢失值
。具体的例子如下：
dic = {'a'：1，'b':2} /print(dic) 得出{'a':1，'b':2} 
print(list(dic))得出['a','b'].解决的方案就是将stats_text_cn(text)的键/值更新到stats_text_en(text)中。
